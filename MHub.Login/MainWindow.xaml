<Window x:Class="MHub.Login.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MHub.Login"
        mc:Ignorable="d"
           Title="Login Page" Height="350" Width="525">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label Content="Username:" Grid.Row="0"/>
            <TextBox Grid.Row="0" Name="UsernameTextBox"/>

            <Label Content="Password:" Grid.Row="1"/>
            <PasswordBox Grid.Row="1" Name="PasswordBox"/>

            <CheckBox Content="Remember Me" Grid.Row="2" Name="RememberMeCheckBox"/>

            <Button Content="Login" Grid.Row="3" Click="LoginButton_Click"/>
            <Button Content="Forgot Password" Grid.Row="4"/>
        </Grid>
    </Window>
